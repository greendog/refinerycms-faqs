<%= render 'refinery/admin/search_header', :url => refinery.faq_admin_faqs_path %>
<div class='pagination_container'>
  <% if @faqs.any? %>
    <%= render 'faqs' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
